'use strict';

/**** Simple Github API layer ****
 * Potentially reusable layer which does not bother about specific
 * models. Could be its own application independent package, and
 * could also be used on other APIs which use the same scheme as
 * Github.
 */
angular.module('github', ['thickm']);
